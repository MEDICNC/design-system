---
componentName: Modal
title: Modal
date: 2024-07-12
published: true
---

<Preview>
  <button type="button" className="uk-btn uk-btn-primary btn-modal">
    토글 버튼
  </button>
  <div id="my-id" uk-modal="true">
    <div className="uk-modal-dialog">
      <div className="uk-modal-header">
        <p className="uk-modal-title">모달 타이틀</p>
      </div>
      <div className="uk-modal-body">모달 바디</div>
      <div className="uk-modal-footer">
        <button className="uk-btn uk-btn-tonal btn-dark">취소</button>
        <button className="uk-btn uk-btn-primary btn-lg">확인</button>
      </div>
      <button className="uk-modal-close btn-icon" type="button">
        <i uk-icon="icon: close-200; ratio:1.33334" className="text-subtle"></i>
      </button>
    </div>
  </div>
</Preview>

```jsx
  <button type="button" className="uk-btn uk-btn-primary btn-modal">
    토글 버튼
  </button>
  <div id="my-id" uk-modal="true">
    <div className="uk-modal-dialog">
      <div className="uk-modal-header">
        <p className="uk-modal-title">모달 타이틀</p>
      </div>
      <div className="uk-modal-body">모달 바디</div>
      <div className="uk-modal-footer">
        <button className="uk-btn uk-btn-tonal btn-dark">취소</button>
        <button className="uk-btn uk-btn-primary btn-lg">확인</button>
      </div>
      <button className="uk-modal-close btn-icon" type="button">
        <i uk-icon="icon: close-200; ratio:1.33334" className="text-subtle"></i>
      </button>
    </div>
  </div>

  <script>
    const modalOpenBtn = document.querySelector('.btn-modal');
    modalOpenBtn.addEventListener('click',function(){
        console.log('')
        if($('#my-id').is('.uk-open')){
            $('#my-id').removeClass('uk-open').hide();
        }else{
            $('#my-id').addClass('uk-open').css('display', 'flex').show();
        }
    });
  </script>
```

<ScriptCode>{`

    const modalOpenBtn = document.querySelector('.btn-modal');
    modalOpenBtn.addEventListener('click',function(){
        console.log('')
        if($('#my-id').is('.uk-open')){
            $('#my-id').removeClass('uk-open').hide();
        }else{
            $('#my-id').addClass('uk-open').css('display', 'flex').show();
        }
    });

`}</ScriptCode>
